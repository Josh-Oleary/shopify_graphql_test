# shopify_graphql_test
### Instructions
  * Pull copy of repo locally
  * Run `npm install`
  * To run the progam, enter `node app.js --name <product-title>`
  * The search returns all products who's title contains the string passed into the command line
